Endpoints

POST /wallets – Create a new wallet
POST /wallets/{id}/assets – Add a crypto asset
GET /wallets/{id} – Get wallet information
POST /wallets/simulate – Simulate wallet performance

Please, don't forget to insert your own created API key in the application.properties file
